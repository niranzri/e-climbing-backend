const router = require("express").Router();
const Product = require("../models/Review.model");

// Endpoint / => /api/reviews

// GET one review

// POST - create one review

// PUT - edit an existing review

// DELETE - delete an existing review
  

module.exports = router;